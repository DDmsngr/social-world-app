/// Функции, которые собраны, но не выходят в релиз.
abstract final class Features {
  /// Личные сообщения.
  ///
  /// Выключены по умолчанию и не попадают в первый MVP сознательно.
  /// Сервис с перепиской Роскомнадзор вправе признать организатором
  /// распространения информации: это год хранения сообщений на территории РФ
  /// и выдача ключей ФСБ по запросу. Пока ООО не зарегистрировано и юрист
  /// не посмотрел на конфигурацию, переписки в продукте нет.
  ///
  /// Код при этом пишется заранее — чтобы потом не переделывать архитектуру
  /// под уже живой продукт.
  ///
  /// Включить для разработки: `--dart-define=FEATURE_CHAT=true`.
  static const chat = bool.fromEnvironment('FEATURE_CHAT');
}
